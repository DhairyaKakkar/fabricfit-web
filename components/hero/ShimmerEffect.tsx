'use client';

import { motion, AnimatePresence } from 'framer-motion';

interface Props {
  isShimmering: boolean;
}

export default function ShimmerEffect({ isShimmering: _ }: Props) {
  return null;
}
